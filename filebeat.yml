filebeat.inputs:
  - type: filestream
    id: docker-logs
    paths:
      - /var/lib/docker/containers/*/*.log
    parsers:
      - container:
          stream: all
          format: auto

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]
  pipeline: "juice-shop-parser"

logging.level: warning
